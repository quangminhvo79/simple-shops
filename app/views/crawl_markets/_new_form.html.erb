<%= turbo_frame_tag :new_crawl_market_frame do %>
  <dialog id="new_crawl_market_dialog" class="modal modal-bottom sm:modal-middle">
    <div class="modal-box">
      <form method="dialog">
        <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
      </form>
      <div class="dialog-header mb-5">
        <h4 class="text-lg font-bold">New Crawl Market</h4>
      </div>
      <div class="form-container">
        <%= render "form", crawl_market: CrawlMarket.new, form_url: crawl_markets_path %>
      </div>
    </div>
  </dialog>
<% end %>
