<div class="text-xl font-bold flex items-center justify-between space-x-2 flex-row cursor-default mb-5">
  <div class="flex items-center space-x-2 flex-row">
    <%= icon "archive" %>
    <span>Crawl Markets</span>
  </div>
  <div>
    <button class="btn" onclick="new_crawl_market_dialog.showModal()">
      <%= icon "plus" %>
      <span>New</span>
    </button>
    <%= render "new_form" %>
  </div>
</div>
<%= render "list" %>
<%= turbo_frame_tag :edit_crawl_market_frame %>
